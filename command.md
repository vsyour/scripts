1. 用谷歌云免费练手 http://ssh.cloud.google.com/
2. 获得公钥和外网IP地址就可以远程登陆了. 

```bash
# 获取密钥和IP地址 端口:6000
source <(curl -sL https://git.io/getgoogle)

# 安装必要的库跑神经网络
pip install tflearn

# 解决vim无法粘贴内容的问题
sed -i 's/mouse=a/mouse-=a/g' /usr/share/vim/vim81/defaults.vim

# 开始执行程序将跑出来日志记录下来
python a.py |tee -a passwd.txt

# 过滤一下再去重
awk '{if(length($0)==10){print $0}}'  passwd.txt >>ok.txt
https://www.ip138.com/quchong/

git clone https://github.com/ffuf/ffuf ; cd ffuf ; go get ; go build
/ffuf/ffuf -u https://static.dingtalk.com/media/FUZZ -w /home/vsyour/ok.txt -e _1080_2259.jpg,_1080_2259.png --mc 200 -c -v -t 100




awk '{if(10 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(11 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(12 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(13 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(14 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(15 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(16 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(17 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(18 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(19 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(20 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(21 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(22 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(23 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(24 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt
awk '{if(25 ==length($0)){print $0}}' password.txt | sort -u >> /home/vsyour/ok.txt


```
